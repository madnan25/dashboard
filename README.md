# Dashboard (Next.js + HeroUI)

## Getting started

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Maintenance

### Lines of code (LOC)

```bash
npm run loc
```

This prints a quick LOC breakdown for app code and Supabase migrations (excluding lockfiles/vendor directories).


